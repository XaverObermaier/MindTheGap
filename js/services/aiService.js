// Placeholder for AI-assisted summarization.
// Today this simply returns the pre-written summary from the data source.
// Swap the body of getSummary() for a real API call once the AI backend
// exists — nothing outside this file needs to change.
export async function getSummary(article) {
  return article.summary;
}
